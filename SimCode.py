#Initialization 
import ParticleClass as par

def init(L, rho, eta, varTB = 0.03):
    """initialized the particles and the lattice
            Args:   L - size of the square shaped cell
                    rho - density of the system, determines the particle number N (rho = N/L**2)
                    eta - noise of the system
                    varTB - absolute velocity at the beginning (default value of the paper 0,03)"""
    pass